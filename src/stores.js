import { writable, readable } from "svelte/store";

export const main = writable(true);
export const rightFacestatus = writable("");
export const menuOpen = writable(false);
export const hire = writable(true);
export const skillPage = writable(false);
export const projectPage = writable(false);
